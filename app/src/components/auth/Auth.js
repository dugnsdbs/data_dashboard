"use client";

import React from "react";
import Form from "../Form";
// import LoginForm from "../LoginForm";

const Auth = () => {
  // return <LoginForm isLogin buttonLabel="Log in" />;
  return <Form isLogin buttonLabel="Log in" />;
};

export default Auth;
